import { InternationalPhoneFormatPipe } from './international-phone-format.pipe';

describe('InternationalPhoneFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new InternationalPhoneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
